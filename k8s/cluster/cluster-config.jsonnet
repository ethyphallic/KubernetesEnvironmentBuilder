function(
  prefix="",
  monitorNamespace="monitor",
  kafkaNamespace="kafka",
  sutNamespace="sut",
  loadNamespace="load",
  infraNamespace="infra"
) {
  prefix: prefix,
  monitorNamespace: monitorNamespace,
  kafkaNamespace: kafkaNamespace,
  sutNamespace: sutNamespace,
  loadNamespace: loadNamespace,
  infraNamespace: infraNamespace
}